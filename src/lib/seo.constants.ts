/**
 * SEO constants shared across routes and the sitemap generator.
 * Single source of truth for the canonical site URL.
 */
export const SITE_URL = "https://leradial.com.ar";

/** Default OG image (logo) — used as fallback when no product image is available. */
export const DEFAULT_OG_IMAGE = `${SITE_URL}/favicon.png`;
